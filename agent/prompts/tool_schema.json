[
  {
    "type": "function",
    "function": {
      "name": "write_file",
      "description": "Write content to a file in the workspace. Creates parent directories if needed. Use this to create or modify files in your project.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Relative path to the file (e.g., 'src/index.ts' or 'package.json')"
          },
          "content": {
            "type": "string",
            "description": "Content to write to the file"
          }
        },
        "required": ["path", "content"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "read_file",
      "description": "Read the contents of a file from the workspace. Use this to inspect existing files or verify what you've written.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Relative path to the file to read (e.g., 'package.json' or 'src/App.tsx')"
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "run_command",
      "description": "Execute a shell command in the workspace. Use this to run build commands, tests, install dependencies, start servers, etc. The command runs with a 5-minute timeout by default.",
      "parameters": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "Shell command to execute (e.g., 'pnpm install' or 'npm run build')"
          },
          "cwd": {
            "type": "string",
            "description": "Optional working directory relative to workspace (defaults to workspace root)"
          }
        },
        "required": ["command"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "install_deps",
      "description": "Install project dependencies using the package manager (pnpm or npm). Automatically uses caching and silent mode. This is more efficient than running 'pnpm install' manually with run_command because it handles package manager detection and uses optimized flags.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "start_server",
      "description": "Start the development server in the background. This runs 'pnpm dev' without blocking, allowing you to continue working. The server will be accessible at http://localhost:3000. Use this instead of running 'pnpm dev' directly with run_command, which would hang forever.",
      "parameters": {
        "type": "object",
        "properties": {
          "port": {
            "type": "integer",
            "description": "Port number for the server (default: 3000)",
            "default": 3000
          }
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "finish_task",
      "description": "Declare that the task is complete. ONLY call this after you have verified the application works by running builds, tests, and/or starting the server successfully. Include a summary of what was accomplished.",
      "parameters": {
        "type": "object",
        "properties": {
          "summary": {
            "type": "string",
            "description": "Brief summary of what was accomplished and how you verified it works"
          }
        },
        "required": ["summary"]
      }
    }
  }
]
